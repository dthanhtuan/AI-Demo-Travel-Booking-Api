# Copy this file to .env before running `docker compose up`
POSTGRES_USER=travel
POSTGRES_PASSWORD=travel
POSTGRES_DB=travel_booking

# Used by the app to connect. When running inside docker compose the host is `db`.
# When running tests/migrations locally against the compose DB, use localhost.
DATABASE_URL=postgres://travel:travel@localhost:5432/travel_booking

PORT=3000
